## DYNAMIC CONFIG
tls:
   certificates:
     - certFile: /certs/thinkcenter.dev.cert.pem
       keyFile: /certs/thinkcenter.dev.key.pem
       stores:
         - default
   stores:
     default:
       defaultCertificate:
         certFile: /certs/thinkcenter.dev.cert.pem
         keyFile: /certs/thinkcenter.dev.key.pem
   options:
     default:
        sniStrict: true
