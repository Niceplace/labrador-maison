# Opencode server username
OPENCODE_SERVER_USERNAME=opencode

# Opencode custom path towards opencode.jsonc
OPENCODE_CONFIG=/etc/opencode/opencode.jsonc

# Opencode custom config directory (for agents, skills, commands)
OPENCODE_CONFIG_DIR=/etc/opencode

# Basic auth for opencode web server
OPENCODE_SERVER_PASSWORD=example-password
